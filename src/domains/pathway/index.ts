import { BufferGeometry } from "three"

/*
function computeGraphExpand(instance: Primitive, distance: number, ...lines: Array<LinePrimitive>): Array<Primitive> {
    return expandGraph(
        createGraph(
            lines.map((line) => [line.getStart(), line.getEnd()]),
            YAXIS,
            distance
        ),
        distance,
        0,
        YAXIS,
        instance.materialGenerator
    )
}

export type Pathway = {
    lines: Array<[x1: number, y1: number, w1: number, x2: number, y2: number, w2: number]>
    geometry?: BufferGeometry
}

function getPathwayGeometry(): BufferGeometry {
    //TODO
}
*/
